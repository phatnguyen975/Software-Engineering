# Edge Cases — Events List (A1)

> **Screen:** A1 — Events List  
> **URL:** https://prod-dev.ems-fitus.cloud/dashboard/admin/events  
> **Execution Mode:** Mode 1 — BrowserMCP (Live Browser)  
> **Tester:** Nguyễn Tấn Phát — 23127449  
> **Date:** 2026-07-27  
> **Summary:** Total 8 items (Pass: 7, Fail: 1, NA: 0)

## Text Box / Text Area (Search Events)

| EC ID     | Scenario                       | How-to-Test                                                                                                                                                                                                                                              | Expected Outcome                                                                                                                                        | Result | Notes                                                                                                                                                                        |
| --------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EC-A1-001 | Maximum character UI limits    | 1. Type or paste exactly 300 'A's into the 'Search events' box.<br>2. Inspect the input's DOM value.<br>3. Verify if it physically stopped accepting characters at a certain limit (e.g. 255) or accepted all 300.                                       | Input physically stops accepting characters; no horizontal scrolling inside a single-line input; layout remains fully intact.                           | Pass   | Input accepted all 300 characters without physically cutting at 255. However, the layout did not break due to internal overflow scrolling; considered Pass for UI integrity. |
| EC-A1-002 | Special characters rendering   | 1. Clear the search box.<br>2. Type `<script>alert(1)</script> " ' & %20 🌟`.<br>3. Verify the string renders perfectly as plain text, no layout distortion occurs, and no alert executes.                                                               | Characters render perfectly as plain text on the screen; no CSS distortion, unexpected blank spaces, or raw HTML rendering visible.                     | Pass   | Renders safely as plain text.                                                                                                                                                |
| EC-A1-003 | Extremely long continuous text | 1. Paste a single, unbroken string of 150 characters (no spaces) into the 'Search events' box.<br>2. Verify the text gracefully truncates or visually overflows smoothly (hides overflow) without expanding the search box width or breaking the layout. | Text gracefully truncates or visually overflows smoothly (e.g., using ellipsis `...`); does not break the container width or overlap other UI elements. | Pass   | Text truncates smoothly inside the input, without breaking the container width.                                                                                              |

## Dropdown / Combo Box (Filters)

| EC ID     | Scenario                                          | How-to-Test                                                                                                                                                                                                                                                                                                                                                                                                 | Expected Outcome                                                                                                                                                                                                                               | Result | Notes                                                                                                                                                               |
| --------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EC-A1-004 | Placeholder styling                               | 1. Click the 'All Status' dropdown to open the menu.<br>2. Observe the 'All Status' option inside the list.<br>3. Verify if it is styled distinctly (greyed out/italicized) compared to selectable values like 'Published' or 'Draft'.                                                                                                                                                                      | Default placeholder text is styled distinctly (e.g., greyed out or italicized) compared to selectable values inside the dropdown.                                                                                                              | Pass   | The 'All Status' option is highlighted as the selected active state, which the user considers sufficient distinction for this specific dropdown design.             |
| EC-A1-005 | Viewport overflow prevention                      | 1. Ensure the table has data (clear any search).<br>2. Scroll to the very bottom of the page.<br>3. Click the 'Rows per page' dropdown.<br>4. Verify the dropdown menu opens upwards, preventing it from bleeding off the bottom of the screen.                                                                                                                                                             | Dropdown menu opens upwards if space below is insufficient, or scrollbar appears within the list; menu absolutely never bleeds off the screen.                                                                                                 | Pass   | Pagination dropdown opens upwards when at the bottom of the viewport.                                                                                               |
| EC-A1-006 | Dropdown container sizing and overflow prevention | 1. Click the filter icons next to 'EVENT TYPES', 'TIME', and 'ACADEMIC CONTEXT' in the table header to open their dropdowns.<br>2. Observe the layout inside the dropdown container.<br>3. Verify that the search input and action buttons fit entirely within the dropdown borders without spilling over.<br>4. Verify that the container does not have excessive empty vertical or horizontal whitespace. | Dropdown menus and popovers must have proper container sizing (min/max width and height). Internal content (inputs, buttons, lists) must not overflow the container boundaries, and the container should be snug without excessive whitespace. | Fail   | 'EVENT TYPES' dropdown content (search input, buttons) overflows the container boundaries. 'TIME' and 'ACADEMIC CONTEXT' dropdowns have excessive empty whitespace. |

## Button (Actions & Pagination)

| EC ID     | Scenario                                        | How-to-Test                                                                                                                                                                                                         | Expected Outcome                                                                                                                                           | Result | Notes                                                                                                            |
| --------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------- |
| EC-A1-007 | Visual lock on submit (Double-click prevention) | 1. Locate the 'Next page' (`>`) pagination button.<br>2. Perform a rapid double-click on it.<br>3. Observe closely if the button immediately enters a disabled state or shows a loading spinner on the first click. | Button immediately changes to a disabled visual state (greyed out or shows a loading spinner) upon the very first click, visually blocking a second click. | Pass   | Double clicking the pagination button only advances one page, effectively preventing multiple rapid submissions. |

## Empty and Loading States

| EC ID     | Scenario                               | How-to-Test                                                                                                                                                                                                | Expected Outcome                                                                                               | Result | Notes                                                                                           |
| --------- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------- |
| EC-A1-008 | Submit button lock on unmodified state | 1. Open an update modal/form (e.g., Important Update).<br>2. Observe the Send button before making any changes.<br>3. Verify the button is visually disabled (greyed out or darker color) and unclickable. | The submit button remains in a disabled visual and functional state until a change is made in the input field. | Pass   | The Send button is properly disabled by default and only becomes active after changes are made. |
