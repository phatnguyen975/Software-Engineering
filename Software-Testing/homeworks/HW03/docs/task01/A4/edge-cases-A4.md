# Edge Cases — A4 (Participants & Reviews Approval)

> **Screen:** A4 — Participants & Reviews Approval  
> **URL:** https://prod-dev.ems-fitus.cloud/dashboard/admin/events/views?id=39  
> **Tester:** Nguyễn Tấn Phát — 23127449  
> **Date:** 2026-07-30  
> **Summary:** Total 6 items (Pass: 5, Fail: 1, NA: 0)

## Screen-Specific Edge Cases

| ID        | Description                                                                      | Expected Outcome                                                                                                                                                                                                                                         | How-to-Test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Result | Notes                                                                                                                                                            |
| --------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EC-A4-001 | Exporting an empty Registrants list                                              | The Export button is disabled, or clicking it downloads an empty template (headers only) without triggering an application error or crash.                                                                                                               | 1. Navigate to the A4 screen and open the Registrants tab.<br>2. In the Search bar, type a string guaranteed not to match any record (e.g., "xyznonexistent123") so the table shows zero results.<br>3. Observe the Export button state — check if it becomes disabled (greyed out) when the list is empty.<br>4. If the Export button remains enabled, click it.<br>5. Verify that either: (a) the button is disabled and shows a tooltip/hint explaining why, or (b) clicking triggers a download of a CSV/Excel file containing only the column headers with no data rows, and no application error or blank page occurs.                                                                                                                                       | Pass   | The Export button remains enabled; clicking it correctly downloads a file containing only headers with no data rows.                                             |
| EC-A4-002 | Extremely long data in Data Table (e.g. very long Email or Note)                 | The text wraps gracefully within the column or truncates with an ellipsis (`...`) showing full text on hover, without horizontally overflowing the cell or breaking the table layout.                                                                    | 1. Navigate to the A4 screen and open the Registrants tab.<br>2. Clear the STATUS filter (click `x`) to reveal all rows.<br>3. Inspect the EMAIL and ROLE columns — check if any cell contains a particularly long value.<br>4. In the Review Lecturers and Review Students tabs, inspect the NOTE column for any long text values.<br>5. Open DevTools → Elements and inspect a long-text cell: verify `overflow: hidden` with `text-overflow: ellipsis` OR `word-break: break-word` is applied in the cell's CSS.<br>6. Hover over a truncated cell (if ellipsis is applied) — verify the full text appears on hover via `title` attribute or a tooltip.<br>7. Confirm that no cell overflows its column boundary or forces a horizontal scrollbar on the table. | Pass   | Extremely long text in columns is properly truncated with ellipsis (`...`) and shows full text on hover.                                                         |
| EC-A4-003 | Switching tabs while an asynchronous action (e.g. Approve/Reject) is in progress | The action completes successfully in the background and updates the data state correctly, without causing UI errors or data desynchronization when navigating back to the tab.                                                                           | 1. Navigate to the A4 screen and open the Review Students tab.<br>2. In the ACTION segmented control, click the right section to switch a student from Pending to Approved.<br>3. Click the Apply button and immediately (within ~1 second) click the Registrants tab before the API response returns.<br>4. Wait for both actions to settle (allow 3–5 seconds).<br>5. Navigate back to the Review Students tab.<br>6. Verify: the approval action was applied correctly (the student's row is removed from the table), and no JavaScript error, spinner freeze, or data mismatch is visible.                                                                                                                                                                     | Pass   | Tested rapid tab switching during an ongoing API request. The action resolves correctly in the background and tab data remains synchronized without UI glitches. |
| EC-A4-004 | Approving a lecturer when Max Slots capacity is reached                          | If the Admin tries to approve a Lecturer when the lecturer capacity is 100% full (e.g., max 2), the system prevents the action and shows a clear warning toast/modal, rather than silently failing.                                                      | 1. Navigate to the A4 screen and open the Review Lecturers tab.<br>2. Note that the event has a limit of 2 lecturers, and currently there is 1 pending.<br>3. Use User Management to create another lecturer account and register them for this event so there are >2 pending lecturers.<br>4. Attempt to approve enough lecturers to exceed the max capacity of 2 (by selecting the Approved section of the control and clicking Apply).<br>5. Verify the system responds with a clear warning toast or blocking modal (e.g., "Capacity reached") rather than silently failing.                                                                                                                                                                                   | Pass   | The system handles max capacity at the registration level. A third lecturer is successfully blocked from registering when the 2/2 limit is reached.              |
| EC-A4-005 | Inconsistent icon usage in Action segmented control (Approve/Pending/Reject)     | All segments within the Action control should follow a consistent design pattern — either all sections include an icon, or none do — to maintain visual consistency.                                                                                     | 1. Navigate to the A4 screen and open the Review Students tab.<br>2. Observe the ACTION column segmented control (Rejected / Pending / Approved) for a student row.<br>3. Check whether the icon treatment is consistent: if the Rejected segment (left) has an icon, verify that Pending (middle) and Approved (right) also have corresponding icons.<br>4. If only Reject has an icon while Pending and Approve do not, document this as an inconsistency.<br>5. Repeat the same check in the Review Lecturers tab.                                                                                                                                                                                                                                              | Fail   | Inconsistent design detected: The 'Rejected' segment contains a leading icon, whereas the 'Pending' and 'Approved' segments only contain text.                   |
| EC-A4-006 | Apply button disabled state when no review actions are selected                  | The Apply button must remain in a visually inactive state (muted background, pointer-events disabled or cursor: not-allowed) when all rows are still set to Pending. It should only become active when at least one row is changed to Approve or Reject. | 1. Navigate to the A4 screen and open the Review Students tab.<br>2. Observe the Apply button when all rows in the ACTION column are in the default Pending state.<br>3. Verify the Apply button shows a visually inactive appearance (e.g., muted/greyed background).<br>4. Hover the mouse over the Apply button — verify the cursor changes to `not-allowed` (or the button is fully non-interactive).<br>5. Click the left (Reject) or right (Approve) section of the ACTION control for a student.<br>6. Verify the Apply button immediately transitions to an active state with full color and a normal `pointer` cursor on hover.<br>7. Repeat steps 2–6 for the Review Lecturers tab.                                                                      | Pass   | Apply button remains visually disabled (muted opacity, not-allowed cursor) until at least one pending action is changed.                                         |
