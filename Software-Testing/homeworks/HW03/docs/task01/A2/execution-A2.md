# Execution Checklist — Add / Edit Event Form (A2)

> **Screen:** A2 — Add / Edit Event Form  
> **Add Screen URL:** https://prod-dev.ems-fitus.cloud/dashboard/admin/events/create  
> **Edit Screen URL:** https://prod-dev.ems-fitus.cloud/dashboard/admin/events/edit?id=39  
> **Execution Mode:** Mode 1 — BrowserMCP (Live Browser)  
> **Tester:** Nguyễn Tấn Phát — 23127449  
> **Date:** 2026-07-29  
> **Summary:** Total 62 items (Pass: 25, Fail: 15, NA: 22)

## IA-01 — General UI Standards

| ID        | Description                                                                                                                                                                                                                                       | Widget / Area            | How-to-Test                                                                                                                                                                                                                        | Result | Notes                                                                                                                                                |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| IA-01-001 | All page-level content is contained within a consistent maximum-width container (e.g., a centered grid); no element visually bleeds out of this defined boundary on large screens.                                                                | Page layout              | 1. Resize the browser window to a very large width (e.g., >1440px).<br>2. Observe the main form container holding Basic Information, Date & Time, etc.<br>3. Verify no component bleeds out of the defined maximum-width boundary. | Pass   | Container width is 1664px (<1920px), no bleeding out.                                                                                                |
| IA-01-002 | Text elements (headings, body, labels, captions) use exactly the fonts and weights defined in the design system; no browser-default serif font appears anywhere on the page.                                                                      | Typography               | 1. Inspect the font styles of Event Title label, placeholder texts, and bottom Action buttons (Save as Draft, Publish).<br>2. Verify no browser-default serif font is used.                                                        | Pass   | Uses sans-serif fonts natively (ui-sans-serif, system-ui).                                                                                           |
| IA-01-003 | Normal body text has a colour-contrast ratio of at least 4.5:1 against its background; large text (>= 18 pt or >= 14 pt bold) has a contrast ratio of at least 3:1.                                                                               | Typography               | 1. Inspect normal text (e.g., "Supported any file format", switch descriptions) and large text (e.g., section headers like "Basic Information").<br>2. Verify contrast ratio >= 4.5:1 for normal and >= 3:1 for large.             | Fail   | Contrast ratio of text in Publish button is 2.08:1, which is below the 4.5:1 requirement.                                                            |
| IA-01-004 | Essential UI component boundaries (e.g., input field outlines) and meaningful icons have a contrast ratio of at least 3:1 against the adjacent background; decorative borders on components with sufficient text contrast are exempt.             | UI components            | 1. Inspect the borders of text inputs (Title, Location), Date pickers, and Dropdowns.<br>2. Inspect icons (e.g., RTE toolbar, Header icons).<br>3. Verify contrast >= 3:1 against background.                                      | Pass   | Component borders and icons meet the minimum 3:1 contrast ratio against their adjacent backgrounds.                                                  |
| IA-01-005 | Every image on the page renders without blur, pixelation, or distortion; no broken-image placeholder icon is visible.                                                                                                                             | Image                    | 1. Upload a valid image to the Thumbnail and Banner dropzones.<br>2. Check if the preview renders clearly without blur, distortion, or broken placeholder.                                                                         | Pass   | Image preview renders clearly without broken placeholder.                                                                                            |
| IA-01-006 | Images (thumbnails, banners, avatars) maintain their expected aspect ratio at every breakpoint; no cropping, letterboxing, or stretching occurs at any grid column width.                                                                         | Image                    | 1. Upload a valid image to Thumbnail and Banner.<br>2. Resize the browser window.<br>3. Verify aspect ratio (4:3, 24:9) is maintained at different column widths.                                                                  | Pass   | Aspect ratio is maintained upon uploading dummy image.                                                                                               |
| IA-01-007 | Typography spacing: Line height within paragraphs is at least 1.5 times the font size, and spacing between paragraphs is visually distinct, ensuring comfortable readability without text feeling cramped.                                        | Typography               | 1. Type multiple lines of text into the "Sub-description" textarea.<br>2. Observe line height (>=1.5x) and paragraph spacing for comfortable reading.                                                                              | Fail   | Line height is 20px with font size 14px (ratio = 20/14 = 1.42), which is less than the required 1.5 multiplier.                                      |
| IA-01-008 | Meaningful images display a descriptive text alternative (alt text) when hovered, or when the image fails to load, ensuring the content is still understandable.                                                                                  | Image                    | 1. Upload a valid image to Thumbnail.<br>2. Hover over the image preview.<br>3. Inspect the DOM element to verify alt text is present.                                                                                             | Pass   | Alt text found on image preview.                                                                                                                     |
| IA-01-009 | Loading state: a visible spinner, skeleton screen, or progress indicator is displayed during asynchronous data-fetch operations; the indicator disappears once data is ready.                                                                     | Loading state            | 1. Throttle network to Slow 4G in DevTools.<br>2. Refresh the page.<br>3. Observe if a loading spinner or skeleton appears before the form fully loads.                                                                            | Pass   | A loading effect (spinner/indicator) is visible during asynchronous data fetch when reloading.                                                       |
| IA-01-010 | Empty state: When a list, table, or search result contains zero items, a clear visual indicator (e.g., text message or illustration) is displayed in the content area, preventing the UI from appearing broken or blank.                          | Empty state              | 1. Observe the initial state of the Thumbnail, Banner, and Attachments zones before any upload.<br>2. Verify they display clear empty-state text (e.g., "No thumbnail image") and instructions.                                    | Pass   | Empty state texts 'No thumbnail image' are visible.                                                                                                  |
| IA-01-011 | Interactive elements (buttons, links, input fields) have a minimum touch/click target size of 24x24 CSS pixels.                                                                                                                                   | All interactive controls | 1. Inspect the upload image buttons (action buttons in dropzones), all RTE toolbar buttons, and Registration Switches.<br>2. Verify each has a minimum touch target size of 24x24 px.                                              | Pass   | Checked via DOM, switch/buttons are at least 24x24 px.                                                                                               |
| IA-01-012 | Carousel components display navigation controls (previous/next arrows and pagination dots) that are visually distinct from the background slide content (e.g., using shadows or contrast borders) ensuring they are not lost against busy images. | Carousel                 |                                                                                                                                                                                                                                    | NA     | No carousel on this page.                                                                                                                            |
| IA-01-013 | When the language is switched (e.g., EN/VI) via the header toggle, all visible UI text updates within one render cycle with no text overflow, truncation, or layout shift.                                                                        | i18n toggle              |                                                                                                                                                                                                                                    | NA     | Tested in A1.                                                                                                                                        |
| IA-01-014 | Carousel components provide keyboard-accessible controls: Left/Right arrow keys (or equivalent buttons reachable by Tab) advance the slides, and each slide's focusable content is reachable by Tab without being trapped inside the carousel.    | Carousel                 |                                                                                                                                                                                                                                    | NA     | No carousel on this page.                                                                                                                            |
| IA-01-015 | The page remains fully usable (no horizontal scrollbar, no content overlap, no truncated text) when the browser is zoomed to 200% or when the viewport is narrowed to 320 CSS px wide.                                                            | Page layout              | 1. Zoom browser to 200%.<br>2. Observe all form sections, upload zones, and the bottom Action buttons for layout breaks.<br>3. Set viewport to 320px wide.<br>4. Observe text inputs, pickers, and switches for responsiveness.    | Fail   | At 320px width, the sidebar is not hidden into a toggle menu, and layout components overflow chaotically instead of staying within their containers. |
| IA-01-016 | Dark/Light mode toggle updates all UI colours seamlessly without unreadable low-contrast text, inverted images, or unreadable borders.                                                                                                            | Theme toggle             |                                                                                                                                                                                                                                    | NA     | No theme toggle on this page.                                                                                                                        |
| IA-01-017 | Spacing and Alignment: UI elements maintain consistent margins/paddings across the page, and inline elements (e.g., icon and text, adjacent buttons) are accurately vertically aligned to each other without jagged offsets.                      | Layout & Alignment       | 1. Observe the alignment of the 4 Date & Time pickers in their grid layout.<br>2. Check vertical alignment of Switches in the Registration section and their adjacent labels.                                                      | Pass   | Layout is consistent, and alignment/gaps between components are visually even and synchronized.                                                      |

## IA-02 — Forms

| ID        | Description                                                                                                                                                                                                              | Widget / Area    | How-to-Test                                                                                                                                                                                                            | Result | Notes                                                                                                                                                                      |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IA-02-001 | Every required form field displays a red asterisk (`*`) immediately adjacent to its label text; optional fields display no asterisk.                                                                                     | Text input       | 1. Check labels for "Event Title", "Start Date & Time", "End Date & Time", "Check-in Open", "Check-in Close", "Registration Open", "Registration Close", and "Campus".<br>2. Verify they display a red asterisk (`*`). | Fail   | The required asterisk (`*`) is black instead of red, failing to visually highlight its mandatory nature.                                                                   |
| IA-02-002 | Every form field has a persistent visible label positioned above or to the left of the field; placeholder text alone is not used as a label.                                                                             | Text input       | 1. Verify all inputs (Event Title, Location, Reminder before hours, etc.) and dropdowns have visible labels above or to the left, not just placeholder text.                                                           | Pass   | Labels are explicitly visible above inputs.                                                                                                                                |
| IA-02-003 | When a user leaves a required field blank and moves focus to the next field, an inline error message appears immediately below that field without waiting for form submission.                                           | Text input       | 1. Click into "Event Title" and leave it blank.<br>2. Press Tab to move focus to the next field.<br>3. Repeat for required Date pickers and "Campus" dropdown.<br>4. Verify inline error appears immediately.          | Fail   | No inline error appears immediately on blur when leaving empty.                                                                                                            |
| IA-02-004 | Each inline error message includes a specific corrective suggestion (e.g., "Email is required" or "End date must be after start date") rather than a generic message such as "Invalid input".                            | Error message    | 1. Click into "Event Title" and leave it blank, then click outside.<br>2. Click "Publish" with "Start Date & Time" empty.<br>3. Verify the error message text is specific (e.g., "Title is required").                 | Pass   | Displays clear and specific error messages under input fields (e.g., "Title is required", "Campus is required").                                                           |
| IA-02-005 | All fields with validation errors are visually highlighted simultaneously after a failed form submission attempt; focus moves to the first erroneous field.                                                              | Form             | 1. Leave all required fields blank.<br>2. Click "Publish" button.<br>3. Observe if all required fields are highlighted simultaneously and focus moves to "Event Title".                                                | Fail   | Inconsistent error styling: all required fields show error text, but only Campus highlights its border in red beside the error text. Focus correctly moves to Event Title. |
| IA-02-006 | Dropdown fields display a visible arrow indicator and open their option list on click or on Enter key press; the selected value is displayed in the field after selection.                                               | Dropdown         | 1. Observe "Event Types", "Academic Context", and "Campus" dropdowns.<br>2. Verify they display a visible arrow indicator.                                                                                             | Fail   | The Campus field only displays placeholder text ("Select campus") without a dropdown arrow indicator.                                                                      |
| IA-02-007 | Date/time picker fields enforce logical range constraints (e.g., end date cannot be earlier than start date); invalid selections are visually prevented or trigger clear inline errors.                                  | Date/time picker | 1. Select a "Start Date & Time".<br>2. Select an "End Date & Time" earlier than the Start Date.<br>3. Repeat for Check-in and Registration times.<br>4. Verify logical constraints are enforced visually.              | Pass   | Logical range constraints are enforced; displays an inline error when End Date is earlier than Start Date.                                                                 |
| IA-02-008 | Date/time picker fields are keyboard accessible: a user can enter a date/time via keyboard without opening the calendar picker widget; the picker itself is also operable by keyboard alone.                             | Date/time picker | 1. Tab to "Start Date & Time".<br>2. Attempt to type a date directly via keyboard.<br>3. Attempt to use arrow keys/Enter to select a date in the calendar popup.                                                       | Fail   | Keyboard input is blocked; the date picker can only be opened and operated by clicking the calendar icon.                                                                  |
| IA-02-009 | Image and file upload controls explicitly display their constraints (e.g., maximum file size, accepted formats) as visible helper text near the button/dropzone to prevent user errors before uploading.                 | File upload      | 1. Observe the helper text in Thumbnail, Attachments, and Banner dropzones.<br>2. Verify constraints (e.g., ratio 4:3, 24:9, supported formats) are explicitly displayed.                                              | Pass   | Constraints like 4:3 and 24:9 are explicitly displayed.                                                                                                                    |
| IA-02-010 | Drag-and-drop file upload zones visually change state (e.g., highlighted border or background colour) when a file is dragged over them, clearly indicating that the drop action is supported.                            | File upload      | 1. Drag a file from OS and hover it over the Thumbnail, Attachments, and Banner dropzones.<br>2. Verify the zone visually changes state (e.g., highlighted border).                                                    | Fail   | None of the upload zones support OS drag-and-drop (despite "drag and drop" text on Attachments) and there is no visual hover reaction.                                     |
| IA-02-011 | When a file upload fails validation, the resulting error message is displayed immediately adjacent to the specific upload control, rather than at the top of the form or in a generic toast.                             | File upload      | 1. Upload a file exceeding the implicit size limit or unsupported format into the Thumbnail zone.<br>2. Verify error message is displayed adjacent to the upload control.                                              | Pass   | The OS file picker natively filters supported formats (e.g., hiding `.pdf`/`.md` for image uploads) preventing invalid selection upfront.                                  |
| IA-02-012 | The Rich Text Editor (RTE) formatting buttons provide immediate visual feedback (WYSIWYG) by correctly changing the appearance of the text when clicked; additionally, all icons display clear text tooltips on hover.   | Rich text editor | 1. Type text into the Description RTE.<br>2. Highlight text and click formatting buttons (P, Font size, etc.).<br>3. Verify WYSIWYG formatting and hover over buttons to check tooltips.                               | Pass   | RTE formatting functions work stably, and immediate visual tooltips appear upon hovering over toolbar buttons.                                                             |
| IA-02-013 | After an image is selected for upload, a visual thumbnail preview is displayed inline within the form, accompanied by a clear, accessible control (e.g., a 'Trash' icon or 'Remove' button) to discard it.               | File upload      | 1. Upload a valid image into the Thumbnail and Banner zones.<br>2. Verify a thumbnail preview is displayed.<br>3. Look for a discard/remove control to delete it.                                                      | Fail   | When uploading an image inside the Rich Text Editor, it only provides a preview with no accessible control (Trash/Remove) to discard it.                                   |
| IA-02-014 | The Rich Text Editor (RTE) area has a defined minimum height and clear visual boundaries; when user-entered content exceeds this height, the area expands gracefully or provides an internal scrollbar without breaking. | Rich text editor | 1. Paste a very long block of text into the Description RTE.<br>2. Observe if the RTE expands gracefully or provides an internal scrollbar without breaking layout.                                                    | Fail   | Extremely long text expands the RTE height infinitely without an internal scrollbar, forcing the user to scroll the entire page to reach the next input section.           |
| IA-02-015 | The primary commit action (e.g., Publish, Submit) is visually distinct from secondary actions (e.g., Save Draft, Cancel) using different button styles, colours, or labels.                                              | Action buttons   | 1. Compare visual styles of "Publish" (solid blue), "Save as Draft" (outlined), and "Preview Event" (outlined).<br>2. Verify the primary action is visually distinct.                                                  | Pass   | Primary action (Publish, blue) is distinct from secondary (Save Draft, outlined).                                                                                          |
| IA-02-016 | Password fields provide a visibility toggle (eye icon) that clearly changes state when clicked, allowing the user to view the masked password.                                                                           | Password input   |                                                                                                                                                                                                                        | NA     | No password fields on this form.                                                                                                                                           |

## IA-03 — Navigation

| ID        | Description                                                                                                                                                                                                        | Widget / Area     | How-to-Test                                                                                                                                                                         | Result | Notes                                                                                                                             |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------- |
| IA-03-001 | The sidebar navigation menu is present and positioned consistently (same location, same width) on every authenticated page of the application.                                                                     | Sidebar nav       |                                                                                                                                                                                     | NA     | Tested in A1.                                                                                                                     |
| IA-03-002 | The active/current page item in the sidebar is visually distinguished from inactive items (e.g., highlighted background, bold text, or left-border indicator) without relying solely on colour.                    | Sidebar nav       |                                                                                                                                                                                     | NA     | Tested in A1.                                                                                                                     |
| IA-03-003 | When the sidebar navigation is collapsed (if supported), icons clearly represent the menu items and display tooltips on hover to identify the section.                                                             | Sidebar nav       |                                                                                                                                                                                     | NA     | Tested in A1.                                                                                                                     |
| IA-03-004 | All sidebar menu items are reachable by pressing Tab (or Arrow keys within a nav landmark) without a mouse; a focus indicator is clearly visible on each focused item.                                             | Sidebar nav       |                                                                                                                                                                                     | NA     | Tested in A1.                                                                                                                     |
| IA-03-005 | Breadcrumb trail accurately reflects the current page location; each breadcrumb segment except the last is a working hyperlink that navigates to the corresponding ancestor page.                                  | Breadcrumb        |                                                                                                                                                                                     | NA     | Tested in A1.                                                                                                                     |
| IA-03-006 | Tab panels (e.g., settings categories, data views) correctly display the corresponding content panel when each tab is clicked; the active tab is visually distinct.                                                | Tab panel         |                                                                                                                                                                                     | NA     | No tab panels on this page.                                                                                                       |
| IA-03-007 | Tab panels are keyboard accessible: Tab key enters the tab list, Left/Right arrow keys move between tabs, Enter or Space key activates the focused tab.                                                            | Tab panel         |                                                                                                                                                                                     | NA     | No tab panels on this page.                                                                                                       |
| IA-03-008 | Drag-and-drop reorder interfaces display a visually distinct drag handle; the dragged item shows reduced opacity (approximately 50%) and a ghost image during drag.                                                | Drag-and-drop     |                                                                                                                                                                                     | NA     | No drag-and-drop on this page.                                                                                                    |
| IA-03-009 | All drag-and-drop reorder functionality is operable by keyboard alone via an explicit alternative mechanism (e.g., Move Up / Move Down buttons); the drag gesture is not the only way to reorder.                  | Drag-and-drop     |                                                                                                                                                                                     | NA     | No drag-and-drop on this page.                                                                                                    |
| IA-03-010 | Every page that navigates away from a form or multi-step flow provides a clearly labelled Back / Cancel control that returns the user to the previous screen.                                                      | Back / Cancel     | 1. Observe the explicit Back icon next to the "Create Event" page title.<br>2. Click it and verify it returns to the Events List (A1) without error.                                | Pass   | Clicking the explicit Back icon returns the user to the Events List (A1) screen correctly.                                        |
| IA-03-011 | Sticky headers, floating toolbars, or fixed overlays do not obscure the currently focused interactive element when navigating via keyboard (the browser must automatically scroll the element into view).          | Keyboard nav      | 1. Press Tab through the form until the page scrolls down.<br>2. Check if the Header obscures the currently focused element.                                                        | Pass   | Keyboard tab navigation scrolls the page appropriately, keeping the focused element visible without being obscured by the header. |
| IA-03-012 | Navigating directly to a deep-linked URL renders the correct content without requiring the user to navigate from the home page; the breadcrumb trail and sidebar active state visually match the deep-linked page. | Deep links        | 1. Load the direct URL `https://prod-dev.ems-fitus.cloud/dashboard/admin/events/create`.<br>2. Verify the correct form renders without requiring navigation from home page.         | Pass   | Direct deep link accesses the form without requiring home navigation.                                                             |
| IA-03-013 | The Tab key order across each page follows the visual reading order (top-to-bottom, left-to-right); no focusable element is skipped, and focus does not jump unexpectedly to a distant section of the page.        | Focus / tab order | 1. Start from the top and press Tab repeatedly.<br>2. Verify focus follows logical reading order (Basic Info → Date & Time → Categories → Registration → Actions) without skipping. | Pass   | Tab order strictly follows the visual reading order (top-to-bottom, left-to-right) with clear focus rings on each component.      |
| IA-03-014 | Pagination controls highlight the current page number and visually disable the 'Previous' button on the first page and the 'Next' button on the last page.                                                         | Pagination        |                                                                                                                                                                                     | NA     | No pagination on this page.                                                                                                       |

## IA-04 — Feedback / State

| ID        | Description                                                                                                                                                                                                                                                                                                                                  | Widget / Area       | How-to-Test                                                                                                                                                                                                          | Result | Notes                                                                                                                                                                                     |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IA-04-001 | Toast notifications appear in a consistent screen position on every page. Success/info toasts auto-dismiss after >= 5 seconds; error/warning toasts do not auto-dismiss or remain for >= 10 seconds. All include a manual dismiss (`x`) button.                                                                                              | Toast               | 1. Fill all required fields validly and click "Save as Draft".<br>2. Observe the resulting toast notification for auto-dismissal (>=5s) and manual dismiss (x) button.                                               | Fail   | No toast notifications appear upon clicking Save as Draft or Publish; the app silently navigates to the Event List.                                                                       |
| IA-04-002 | Toast notifications use consistent colour semantics: green for success, red for error, yellow/amber for warning, blue for informational; the colour is accompanied by a text label or icon that conveys the same semantic meaning independently of colour.                                                                                   | Toast               | 1. Trigger a success toast (Save as Draft).<br>2. If possible, trigger a network error toast.<br>3. Verify color semantics (e.g., green for success) and accompanying icon/label.                                    | Fail   | No toast notifications exist to evaluate semantic colours.                                                                                                                                |
| IA-04-003 | Toast notifications visually animate in and out smoothly without abruptly shifting other page content or breaking the layout.                                                                                                                                                                                                                | Toast               | 1. Observe the entrance and exit animation of any toast triggered.<br>2. Verify it animates smoothly without breaking page layout.                                                                                   | Fail   | No toast notifications exist to evaluate animations.                                                                                                                                      |
| IA-04-004 | Status badges (e.g., Active, Pending) include a visible text label or icon in addition to their background colour to convey meaning, ensuring status does not rely solely on colour. Furthermore, badges must have consistent styling (border, padding) and use logical, semantically appropriate colours across all different status types. | Status badge        | 1. Click the "Preview Event" button to open the preview screen.<br>2. Observe the "Preview" badge.<br>3. Verify it has consistent padding, logical colors, and includes a text label.                                | Pass   | The "Preview" badge is visually clear, with appropriate padding and logical colours (darker text on same-palette background).                                                             |
| IA-04-005 | Confirmation dialogs appear before every destructive or irreversible action (e.g., delete record, cancel subscription); each dialog clearly states the action being confirmed and provides labelled Confirm and Cancel buttons.                                                                                                              | Confirmation dialog |                                                                                                                                                                                                                      | NA     | No confirmation dialogs on this page.                                                                                                                                                     |
| IA-04-006 | Modal dialogs trap keyboard focus while open; pressing Esc or clicking the outside overlay dismisses the dialog (if dismissal is permitted), and keyboard focus returns to the triggering element upon closing.                                                                                                                              | Modal dialog        |                                                                                                                                                                                                                      | NA     | No modals on this page.                                                                                                                                                                   |
| IA-04-007 | Progress bars display a numerical value (e.g. "12/30" or "40%") adjacent to the bar; the displayed value visually matches the proportion of the filled bar.                                                                                                                                                                                  | Progress bar        |                                                                                                                                                                                                                      | NA     | No progress bars on this page.                                                                                                                                                            |
| IA-04-008 | Progress bars animate smoothly to reflect changes in value, without visual stuttering or jumping out of their container boundaries.                                                                                                                                                                                                          | Progress bar        |                                                                                                                                                                                                                      | NA     | No progress bars on this page.                                                                                                                                                            |
| IA-04-009 | Successful actions (e.g., save, delete) trigger a clear success notification (toast/message), and the on-screen UI (lists, counters) updates immediately to reflect the new state without requiring a manual page refresh.                                                                                                                   | Async feedback      | 1. Click "Save as Draft".<br>2. Verify if the UI updates immediately to reflect the saved state (e.g., redirects or shows success toast) without manual refresh.                                                     | Pass   | Draft is saved and immediately reflected in the Event List table without requiring a manual page refresh.                                                                                 |
| IA-04-010 | Failed actions trigger an error notification that clearly explains the issue in user-friendly language, rather than displaying raw system exceptions or code-level stack traces on the UI.                                                                                                                                                   | Error feedback      |                                                                                                                                                                                                                      | NA     | Cannot reliably simulate server-side rejection (500 Error) via pure GUI interaction.                                                                                                      |
| IA-04-011 | If an action fails after submission (e.g., server rejection), the form remains open and visually populated with the user's previously entered data, preventing frustrating data loss.                                                                                                                                                        | Form state          | 1. Force a failed submission (DevTools → Network → Offline → Publish).<br>2. Verify the form data previously entered remains populated to prevent data loss.                                                         | Pass   | When the network connection is restored and the page reloads, a confirmation dialog appears allowing the user to restore previously entered form data, successfully preventing data loss. |
| IA-04-012 | Real-time logs or data feeds display new entries with consistent formatting (e.g., timestamp, user name, action) matching the existing list layout without breaking the grid.                                                                                                                                                                | Real-time log       |                                                                                                                                                                                                                      | NA     | No real-time logs on this page.                                                                                                                                                           |
| IA-04-013 | When a data item is removed from a list via real-time update or async deletion, the UI layout updates gracefully (e.g., surrounding items shift up) without leaving a broken layout, misaligned grid, or blank gap.                                                                                                                          | Real-time state     |                                                                                                                                                                                                                      | NA     | No real-time lists on this page.                                                                                                                                                          |
| IA-04-014 | Interactive elements (buttons, links, table rows) display distinct visual feedback states when hovered by a mouse pointer (e.g., darkened colour) and when clicked/pressed (e.g., sunken effect).                                                                                                                                            | Interaction states  | 1. Hover over "Publish", "Save as Draft", and "Preview Event" buttons.<br>2. Click them.<br>3. Verify distinct visual states (e.g., background darkens on hover, button appears sunken or outline changes on click). | Pass   | Buttons exhibit clear visual feedback states for both hover (darkens) and pressed/active (sunken).                                                                                        |
| IA-04-015 | Icon-only buttons (e.g., a standalone Trash or Edit icon) display a clear text tooltip when hovered or focused, and the tooltip does not obscure the icon itself or adjacent interactive elements.                                                                                                                                           | Tooltips            | 1. Hover over the action buttons in the Thumbnail/Banner upload zones.<br>2. Verify clear text tooltips appear without obscuring the icon.                                                                           | Fail   | Hovering over the action buttons in the upload zones does not display any tooltips.                                                                                                       |
