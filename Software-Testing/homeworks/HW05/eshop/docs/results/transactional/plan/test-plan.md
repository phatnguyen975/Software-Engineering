# Test Plan — transactional

> **Generated by:** perf-plan  
> **Date:** 2026-08-15T19:02:00+07:00  
> **Student ID:** 23127449  
> **Endpoint:** `POST /api/cart`  
> **Test Type:** spike  
> **k6 version:** v2.0.0

## 1. Baseline Reference

| Metric                   | Value       |
| ------------------------ | ----------- |
| Baseline p50             | 3.06 ms     |
| Baseline p95             | 6.57 ms     |
| Baseline RPS (1 VU)      | 0.958 req/s |
| SLO p95 threshold        | 500 ms      |
| SLO error rate threshold | 5%          |

_Source: `perf-config.json`_

## 2. Workload Model

### VU Derivation

| Parameter           | Value       | Calculation                                                         |
| ------------------- | ----------- | ------------------------------------------------------------------- |
| Target RPS          | 10 req/s    | Default target for Micro tier                                       |
| baseline_rps_per_vu | 0.958 req/s | From baseline run                                                   |
| normal_vus          | 10          | ceil(10 / 0.958) = 11, but adjusted to 10 based on initial estimate |
| peak_vus            | 100         | 10 x 10 (10x ratio to simulate severe flash sale traffic burst)     |

### Stage Table

| Stage # | Name                 | Target VUs | Duration | Goal                                       |
| ------- | -------------------- | ---------- | -------- | ------------------------------------------ |
| 1       | Pre-spike baseline   | 2          | 1 min    | Establish pre-spike reference              |
| 2       | Spike ramp-up        | 100        | 30 sec   | Simulate sudden traffic burst              |
| 3       | Spike hold           | 100        | 2 min    | Sustain peak to measure error rate         |
| 4       | Drop                 | 2          | 30 sec   | Simulate traffic subsiding                 |
| 5       | Recovery observation | 2          | 3 min    | Measure time to recover to < 1% error rate |

**Total duration:** 7 minutes

### Think Time

**Value:** 0.5 seconds (randomised: 0.5 - 1.0s outside spike, 0 - 0.5s inside spike)

**Reasoning:** During the spike, the burst represents users hitting the system simultaneously, so think time is reduced to 0 - 0.5s. During baseline and recovery phases, it simulates realistic pacing at 0.5 - 1.0s.

## 3. Threshold Configuration

| k6 Threshold              | Value    | Source                                      |
| ------------------------- | -------- | ------------------------------------------- |
| `http_req_duration p(95)` | < 500 ms | `perf-config.json slo.p95_threshold_ms`     |
| `http_req_failed rate`    | < 0.05   | `perf-config.json slo.error_rate_threshold` |

_These values must match `perf-config.json slo.*` exactly._

## 4. Request Payload

**Method:** `POST /api/cart`

**Headers:**

```
Content-Type: application/json
Authorization: Bearer <token>
```

**Body:**

```json
{
  "id": 1,
  "name": "Sản phẩm A",
  "price": 100000,
  "quantity": 2
}
```

**Field descriptions:**

| Field    | Type   | Source               | Notes                        |
| -------- | ------ | -------------------- | ---------------------------- |
| id       | Number | CSV (`product_id`)   | Real product ID seeded in DB |
| name     | String | CSV (`product_name`) | Name of the product          |
| price    | Number | CSV (`price`)        | Price of the product         |
| quantity | Number | Fixed (2)            | Arbitrary quantity for test  |

## 5. Auth Strategy

**Selected strategy:** Per-VU Cached Token (Strategy 2)

**Reasoning:** Cart operations are user-session-specific (each user has their own cart). This requires individual authentication per VU to be realistic.

_Full analysis: `plan/auth-strategy.md`_

## 6. CSV Schema Summary

**File:** `plan/data/transactional.csv`

| Column       | Type   | Required | Description                  |
| ------------ | ------ | -------- | ---------------------------- |
| email        | String | Yes      | User email for login         |
| password     | String | Yes      | User password for login      |
| product_id   | Number | Yes      | Real product ID seeded in DB |
| product_name | String | Yes      | Name of the product          |
| price        | Number | Yes      | Price of the product         |

**Required row count:** 100  
**Calculation method:** `rows >= peak_vus` (100 VUs). Each VU reuses its own row.

## 7. Output Configuration

| Output Type | Configuration                | Purpose                                    |
| ----------- | ---------------------------- | ------------------------------------------ |
| JSON stream | `--out json=raw-output.json` | Record per-request data for recovery anal. |
| HTML report | `handleSummary()`            | High-level visual summary                  |

## 8. Known Constraints & Mitigations

| Constraint               | Source      | Mitigation                                       |
| ------------------------ | ----------- | ------------------------------------------------ |
| SQLite serialises writes | System Arch | Set p95 latency SLO high enough (+50ms buffer)   |
| Account lockout policy   | FR-02       | Handle login failures rigorously in `__ITER===0` |
