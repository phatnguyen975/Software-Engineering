# Performance Analysis Report — {group_name}

> **Generated by:** perf-report  
> **Date:** {report_date}  
> **Test type:** {test_type}  
> **Endpoint:** `{method} {endpoint_path}`  
> **Script:** `{script_filename}`  
> **Run date:** {run_date}

## 1. Metrics Table

> All values sourced from `run/raw/summary.json` unless noted.

| Metric           | Measured Value     | Baseline                   | SLO Threshold           | Delta from Baseline                  |
| ---------------- | ------------------ | -------------------------- | ----------------------- | ------------------------------------ |
| p50 latency      | {p50_ms} ms        | {baseline_p50_ms} ms       | —                       | {p50_delta_ms} ms ({p50_delta_pct}%) |
| p95 latency      | {p95_ms} ms        | {baseline_p95_ms} ms       | < {slo_p95_ms} ms       | {p95_delta_ms} ms ({p95_delta_pct}%) |
| p99 latency      | {p99_ms} ms        | {baseline_p99_ms} ms       | —                       | {p99_delta_ms} ms ({p99_delta_pct}%) |
| Max latency      | {max_ms} ms        | {baseline_max_ms} ms       | —                       | —                                    |
| Error rate       | {error_rate_pct}%  | {baseline_error_rate_pct}% | < {slo_error_rate_pct}% | —                                    |
| RPS (avg)        | {rps} req/s        | {baseline_rps} req/s       | —                       | —                                    |
| Peak VUs         | {peak_vus}         | —                          | —                       | —                                    |
| Total iterations | {total_iterations} | —                          | —                       | —                                    |

**Source fields:**

- p95: `metrics.http_req_duration.values["p(95)"]`
- Error rate: `metrics.http_req_failed.values.rate × 100`
- RPS: `metrics.http_reqs.values.rate`
- Peak VUs: `metrics.vus_max.values.value`

_(For soak tests: endurance metrics are sourced from `run/soak-endurance-report.md`.)_

## 2. SLO Pass / Fail Summary

| Threshold   | Configured Value        | Measured Value    | Outcome           |
| ----------- | ----------------------- | ----------------- | ----------------- |
| p95 latency | < {slo_p95_ms} ms       | {p95_ms} ms       | ✅ PASS / ❌ FAIL |
| Error rate  | < {slo_error_rate_pct}% | {error_rate_pct}% | ✅ PASS / ❌ FAIL |

**Source:** `metrics.{metric_name}.thresholds["{threshold_string}"].ok` — `true` = PASS, `false` = FAIL.

**k6 exit code:** {exit_code} ({exit_code_meaning})

_(Add rows for any additional thresholds configured in the test script.)_

## 3. Phase Commentary

> Interpretation of system behaviour at each test stage. Observed values are cited; interpretations are marked as inferred.

{phase_commentary_content}

_(Written per `references/phase-commentary-guide.md`. One subsection per stage in the test plan.)_

## 4. Key Finding

> One paragraph. The single most important result of this test run.

{key_finding_paragraph}

_(For stress: breaking point VU count. For spike: recovery time. For soak: drift classification. For load: sustained performance verdict.)_

## 5. Performance Recommendations

> Generated by AI from measured data. Feasible / Hallucinated classification is assigned by the human reviewer at Human Gate 1. Each recommendation must cite a specific metric value and its source.

### Recommendation 1: {title}

**Basis:** `{metric_name}` = {value} indicates {specific problem or pattern}. Source: `run/raw/summary.json → metrics.{field_path}`

**Proposed action:** {concrete description}

**Expected impact:** {measurable improvement description}

### Recommendation 2: {title}

**Basis:** {metric_name} = {value} indicates {specific problem or pattern}. Source: {file} → {field_path}

**Proposed action:** {concrete description}

**Expected impact:** {measurable improvement description}

### Recommendation 3: {title}

**Basis:** {metric_name} = {value} indicates {specific problem or pattern}. Source: {file} → {field_path}

**Proposed action:** {concrete description}

**Expected impact:** {measurable improvement description}

_(Add more recommendations as needed. Minimum 3.)_

## 6. Executive Summary

> ⚠️ This section is written AFTER both Human Gates are closed. Do not pre-fill.

### Overall Verdict: {PASS | FAIL | DEGRADED}

{one_sentence_verdict_justification}

### Top 3 Findings

1. {finding_1}
2. {finding_2}
3. {finding_3}

### Confirmed Bugs

{bug_count} confirmed bug(s).

| Bug           | GitHub Issue  |
| ------------- | ------------- |
| {bug_title_1} | {issue_url_1} |

_(If none: "No bugs confirmed at Human Gate 2.")_

### Actionable Recommendations

_(Only recommendations classified Feasible or Partially feasible at Human Gate 1.)_

| #   | Recommendation         | Verdict                       |
| --- | ---------------------- | ----------------------------- |
| {n} | {recommendation_title} | Feasible / Partially feasible |

### Cross-References

- Misinterpretations & recommendation verdicts: [`report/misinterpretations.md`](misinterpretations.md)
- Bug candidates & human verdicts: [`report/bug-candidates.md`](bug-candidates.md)
- Run log: [`run/run-log.md`](../run/run-log.md)
- Raw summary: [`run/raw/summary.json`](../run/raw/summary.json)
