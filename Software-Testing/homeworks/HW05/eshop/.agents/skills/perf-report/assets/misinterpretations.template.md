# Misinterpretations & Recommendation Verdicts — {group_name}

> **Generated by:** perf-report — Step 3  
> **Date:** {report_date}  
> **Test type:** {test_type}  
> **Endpoint:** `{method} {endpoint_path}`  
> **Human reviewer:** {reviewer_name_or_initials}  
> **Review date:** {review_date}

## Part A — AI Misreadings

> This section records every metric value or field that the AI read incorrectly. Human reviewer identified the errors at Human Gate 1. Corrections cite the exact source file and field path.

### Misreading 1 _(if any)_

**What AI claimed:** {ai_claim}

**Correct value:** {correct_value}  
**Source:** `{file_name} → {field_path}`

**Error category:** {category}

Categories:

- `unit_confusion` — e.g. treated decimal fraction as percentage, or ms as seconds
- `metric_name_mismatch` — e.g. used `iteration_duration` instead of `http_req_duration`
- `wrong_aggregation` — e.g. used `avg` instead of `p(95)`
- `wrong_time_window` — e.g. quoted full-test aggregate when phase-specific value was needed
- `field_path_error` — e.g. used `vus.values.value` instead of `vus_max.values.value`
- `other` — describe

_(Add a block for each misreading. If none were identified, state:)_

**No misreadings identified.** The human reviewer confirmed all metric values in `analysis.md` were read correctly from the source files.

## Part B — Recommendation Verdicts

> Human verdict for each recommendation generated in `analysis.md` Section 5. Reasoning is transcribed verbatim from the human reviewer's input — not paraphrased.

### Recommendation 1: {recommendation_title}

**Human verdict:** Feasible / Hallucinated / Partially feasible

**Reason (verbatim):** "{human_reason}"

### Recommendation 2: {recommendation_title}

**Human verdict:** Feasible / Hallucinated / Partially feasible

**Reason (verbatim):** "{human_reason}"

### Recommendation 3: {recommendation_title}

**Human verdict:** Feasible / Hallucinated / Partially feasible

**Reason (verbatim):** "{human_reason}"

_(Add a block for each recommendation in analysis.md Section 5.)_

## Summary

| Category                             | Count                |
| ------------------------------------ | -------------------- |
| Misreadings identified               | {misreading_count}   |
| Recommendations — Feasible           | {feasible_count}     |
| Recommendations — Partially feasible | {partial_count}      |
| Recommendations — Hallucinated       | {hallucinated_count} |
| Total recommendations                | {total_count}        |

## Analysis of AI Error Patterns

> AI-written retrospective: what types of errors were made and why. This section feeds the AI Critique required by the assignment.

**Error pattern observed:** {pattern_description}

**Likely cause:** {cause_description}

**What would prevent this:** {prevention_description}

_(If no misreadings: "No errors were identified in this run. All metric values were correctly read and sourced.")_
