/**
 * tests/config/stages.js
 *
 * Stage definitions sourced from the approved test-plan.md stage table.
 * Do not edit these values directly — update test-plan.md and regenerate.
 *
 * Generated by: perf-build
 * Group: {group_name}
 * Test type: {test_type}
 * Date: {generated_date}
 */

/**
 * k6 stages array — import this in your test script options.
 *
 * Each stage object: { duration: '<value><unit>', target: <vus> }
 * duration units: s (seconds), m (minutes), h (hours)
 * target: VU count at the END of this stage (k6 ramps linearly from the previous target)
 *
 * Usage:
 *   import { stages } from '../../tests/config/stages.js';
 *   export const options = { stages, thresholds };
 */
export const stages = [
  // Stage 1: {stage_1_name} — {stage_1_goal}
  { duration: "{stage_1_duration}", target: { stage_1_vus } },

  // Stage 2: {stage_2_name} — {stage_2_goal}
  { duration: "{stage_2_duration}", target: { stage_2_vus } },

  // Stage N: {stage_n_name} — {stage_n_goal}
  { duration: "{stage_n_duration}", target: { stage_n_vus } },

  // Ramp-down — return to 0 VUs
  { duration: "{rampdown_duration}", target: 0 },
];

/**
 * Derived workload parameters (for reference — not used directly by k6)
 */
export const NORMAL_VUS = { normal_vus }; // plateau VU count
export const PEAK_VUS = { peak_vus }; // maximum VU count reached in any stage
