# Scenario Design — {group_name}

> **Generated by:** perf-spec  
> **Date:** {created_at}  
> **Student ID:** {student_id}  
> **Endpoint:** `{method} {endpoint_path}`  
> **Test Type:** {test_type}

## 1. Endpoint Analysis

### Operation Class

<!-- State the operation class (e.g. Read-Complex, Auth-Intensive) and explain why. -->

**Class:** {operation_class}

**Reasoning:** {operation_class_reasoning}

### Database Involvement

<!-- Describe what database operations this endpoint likely triggers. Reference SRS if business rules constrain the query shape. -->

{db_involvement}

### Authentication Requirement

<!-- State whether auth is required, what mechanism (JWT, session, API key, etc.), and where the credential must be obtained. -->

**Requires auth:** {requires_auth}

{auth_details}

### Business Rules & Constraints

<!-- List any rate limits, lockout policies, concurrency constraints, data integrity rules, or other behaviours described in the SRS that could affect test design. -->

| Rule     | Source (SRS section or API spec) | Impact on Testing |
| -------- | -------------------------------- | ----------------- |
| {rule_1} | {source_1}                       | {impact_1}        |

_(Add rows as needed. If none found, state "No constraints identified" — do not leave blank.)_

## 2. Test Type Justification

**Selected test type:** {test_type}

**Why this test type fits this endpoint:** {test_type_justification}

**Why other test types are less appropriate for this endpoint:** {alternative_types_reasoning}

## 3. Preliminary SLO Targets

> These are estimates based on industry benchmarks and endpoint characteristics. They will be replaced with data-driven values after the baseline run. Reference: `references/slo-benchmarks.md`

| Metric      | Preliminary Target        | Reasoning              |
| ----------- | ------------------------- | ---------------------- |
| p95 latency | {preliminary_p95_ms} ms   | {p95_reasoning}        |
| Error rate  | {preliminary_error_rate}% | {error_rate_reasoning} |

**Hardware tier classification:** {hardware_tier}

**Benchmark reference range used:** {benchmark_range}

## 4. VU Range Estimate

> Rough estimate only — workload model design will produce exact numbers.

| Parameter               | Estimate              | Basis              |
| ----------------------- | --------------------- | ------------------ |
| Normal VUs (plateau)    | {normal_vus_estimate} | {normal_vus_basis} |
| Peak VUs (stress/spike) | {peak_vus_estimate}   | {peak_vus_basis}   |

## 5. Known Risks & Mitigations

<!-- List risks that could make test results misleading and how to mitigate them. -->

| Risk     | Likelihood     | Mitigation     |
| -------- | -------------- | -------------- |
| {risk_1} | {likelihood_1} | {mitigation_1} |

_(Add rows as needed. Common risks: token expiry mid-test, unique data exhaustion, DB lock contention under concurrent writes, account lockout policies.)_

## 6. Open Questions

<!-- Things that need human confirmation before proceeding. -->

- [ ] {open_question_1}

_(Remove this section if there are no open questions.)_

_This document represents the analyst's interpretation of the SRS and API spec. Human review is required before baseline execution._
