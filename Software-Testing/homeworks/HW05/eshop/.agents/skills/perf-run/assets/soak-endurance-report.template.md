# Soak Endurance Report — {group_name}

> **Generated by:** perf-run  
> **Date:** {report_date}  
> **Endpoint:** `{method} {endpoint_path}`  
> **Test duration:** {total_duration_minutes} min  
> **Plateau VUs:** {soak_vus}  
> **Source:** `run/raw/soak-output.json`

Fields marked **[HUMAN]** require values from external monitoring (Grafana, OS resource monitor). These fields cannot be derived from the k6 JSON stream and must be filled by the human operator.

## 1. Endurance Metrics

| Metric            | Value                    | Source                                                 | Calculation                                                        |
| ----------------- | ------------------------ | ------------------------------------------------------ | ------------------------------------------------------------------ |
| max_stable_rps    | {max_stable_rps}         | Computed from JSON                                     | `{plateau_requests} requests / {plateau_duration_s}s`              |
| p95_at_start_ms   | {p95_at_start_ms} ms     | Computed from JSON                                     | p95 of requests in minutes {start_window_start}–{start_window_end} |
| p95_at_end_ms     | {p95_at_end_ms} ms       | Computed from JSON                                     | p95 of requests in minutes {end_window_start}–{end_window_end}     |
| p95_drift_ms      | {p95_drift_ms} ms        | Computed: end − start                                  | {drift_interpretation}                                             |
| error_rate_at_end | {error_rate_at_end_pct}% | Computed from JSON                                     | `{end_failures} / {end_requests}`                                  |
| memory_ceiling_mb | **[HUMAN]**              | Grafana Memory Usage panel — peak value during plateau | —                                                                  |
| degradation_point | **[HUMAN]**              | Visual observation of Grafana p95 trend panel          | Minute X or "none observed"                                        |

## 2. Environment Reference

| Field                  | Value                    |
| ---------------------- | ------------------------ |
| container_cpu_limit    | {container_cpu_limit}    |
| container_memory_limit | {container_memory_limit} |
| host_cpu_cores         | {host_cpu_cores}         |
| host_ram_gb            | {host_ram_gb} GB         |

_Source: `perf-config.json environment`_

## 3. Time Window Analysis

| Window             | Start                | End                    | Request count         | p95 (ms)          | Error rate            |
| ------------------ | -------------------- | ---------------------- | --------------------- | ----------------- | --------------------- |
| Warm-up (excluded) | {plateau_start}      | {plateau_start + 2min} | —                     | —                 | —                     |
| Start window       | {start_window_start} | {start_window_end}     | {start_count}         | {p95_at_start_ms} | {start_error_rate}%   |
| End window         | {end_window_start}   | {end_window_end}       | {end_count}           | {p95_at_end_ms}   | {end_error_rate}%     |
| Full plateau       | {plateau_start}      | {plateau_end}          | {plateau_total_count} | —                 | {plateau_error_rate}% |

## 4. Drift Interpretation

**p95 drift:** {p95_drift_ms} ms ({drift_direction})

| Drift range | Classification                              |
| ----------- | ------------------------------------------- |
| < 10 ms     | Stable — no meaningful degradation          |
| 10–50 ms    | Mild drift — monitor in subsequent runs     |
| ≥ 50 ms     | Significant degradation — investigate cause |

**Classification for this run:** {drift_classification}

**Assessment:** {drift_assessment_text}

## 5. Anomalies Observed in JSON Stream

_(Events or patterns in the JSON stream that deviate from expected behaviour.)_

| Anomaly     | Timestamp     | Detail     |
| ----------- | ------------- | ---------- |
| {anomaly_1} | {timestamp_1} | {detail_1} |

_(State "None observed" if the stream showed no anomalies.)_

## 6. Endurance Threshold Summary

| Threshold                | Value                       | Status                                                            |
| ------------------------ | --------------------------- | ----------------------------------------------------------------- |
| SLO p95 threshold        | {slo_p95_threshold_ms} ms   | p95 at end: {p95_at_end_ms} ms — {WITHIN / EXCEEDED}              |
| SLO error rate threshold | {slo_error_rate_threshold}% | Error rate at end: {error_rate_at_end_pct}% — {WITHIN / EXCEEDED} |
| Memory ceiling           | **[HUMAN to fill]**         | —                                                                 |
| Degradation observed     | **[HUMAN to fill]**         | —                                                                 |

## 7. Human Completion Required

**Instructions for the human operator:**

1. Open your monitoring tool (Grafana Memory Usage panel or OS resource monitor).
2. Find the **peak memory** value during the plateau phase. Fill in:
   - `memory_ceiling_mb`: `___` MB

3. Review the Grafana p95 latency trend panel. Identify the earliest point where p95 began a sustained upward trend. Fill in:
   - `degradation_point`: Minute `___` / "none observed"

4. Update the "Endurance Threshold Summary" table rows marked `[HUMAN to fill]`.
5. Verify the AI-computed values (max_stable_rps, p95_drift_ms, error_rate_at_end) against your own observations from the run.
6. Approve this document.

> **Document approved by:** `___` **Date:** `___`
