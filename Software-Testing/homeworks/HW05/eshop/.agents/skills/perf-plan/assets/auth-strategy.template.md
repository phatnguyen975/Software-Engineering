# Auth Strategy — {group_name}

> **Generated by:** perf-plan  
> **Date:** {created_at}  
> **Endpoint:** `{method} {endpoint_path}`  
> **Test Type:** {test_type}

## 1. Endpoint Auth Requirement

**Requires authentication:** {requires_auth}

**Auth mechanism:** {auth_mechanism}

_(e.g. JWT Bearer token obtained via POST /api/login)_

**Session scope:** {session_scope}

_(e.g. "Response is specific to authenticated user" or "Response is user-independent")_

**JWT TTL:** {jwt_ttl}

_(State the known TTL, or "Unknown — assumed > test duration" if not documented in the API spec)_

## 2. Strategy Evaluation

### Strategy 1 — `setup()` Shared Token

| Dimension         | Assessment for this endpoint  |
| ----------------- | ----------------------------- |
| Login burden      | {strategy1_login_burden}      |
| Session realism   | {strategy1_session_realism}   |
| Token expiry risk | {strategy1_token_expiry_risk} |
| CSV impact        | {strategy1_csv_impact}        |
| **Verdict**       | {strategy1_verdict}           |

**Rejected / Selected because:** {strategy1_rationale}

### Strategy 2 — Per-VU Cached Token

| Dimension         | Assessment for this endpoint  |
| ----------------- | ----------------------------- |
| Login burden      | {strategy2_login_burden}      |
| Session realism   | {strategy2_session_realism}   |
| Token expiry risk | {strategy2_token_expiry_risk} |
| CSV impact        | {strategy2_csv_impact}        |
| **Verdict**       | {strategy2_verdict}           |

**Rejected / Selected because:** {strategy2_rationale}

### Strategy 3 — Pre-Generated Token in CSV

| Dimension         | Assessment for this endpoint  |
| ----------------- | ----------------------------- |
| Login burden      | {strategy3_login_burden}      |
| Session realism   | {strategy3_session_realism}   |
| Token expiry risk | {strategy3_token_expiry_risk} |
| CSV impact        | {strategy3_csv_impact}        |
| **Verdict**       | {strategy3_verdict}           |

**Rejected / Selected because:** {strategy3_rationale}

## 3. Selected Strategy

**Strategy:** {selected_strategy_name}

**Summary of reasoning:** {selected_strategy_full_reasoning}

## 4. Implementation Requirements

The following constraints must be reflected in the test script built from this plan.

| Requirement            | Detail                      |
| ---------------------- | --------------------------- |
| CSV columns required   | {required_csv_columns}      |
| k6 pattern to use      | {k6_pattern}                |
| Login tagging          | {login_tagging_requirement} |
| Login failure handling | {login_failure_handling}    |
| Token refresh needed   | {token_refresh_needed}      |

_(Populate from `references/auth-strategy.md` Implementation Notes for the selected strategy.)_

## 5. Human Gate Review Notes

_(AI populates this section with any change requests made at Human Gate 1 and how they were addressed.)_

{human_gate_notes}
