# Test Plan — {group_name}

> **Generated by:** perf-plan  
> **Date:** {created_at}  
> **Student ID:** {student_id}  
> **Endpoint:** `{method} {endpoint_path}`  
> **Test Type:** {test_type}  
> **k6 version:** {k6_version}

## 1. Baseline Reference

| Metric                   | Value                       |
| ------------------------ | --------------------------- |
| Baseline p50             | {baseline_p50_ms} ms        |
| Baseline p95             | {baseline_p95_ms} ms        |
| Baseline RPS (1 VU)      | {baseline_rps} req/s        |
| SLO p95 threshold        | {slo_p95_threshold_ms} ms   |
| SLO error rate threshold | {slo_error_rate_threshold}% |

_Source: `perf-config.json`_

## 2. Workload Model

### VU Derivation

| Parameter           | Value                | Calculation              |
| ------------------- | -------------------- | ------------------------ |
| Target RPS          | {target_rps} req/s   | {target_rps_reasoning}   |
| baseline_rps_per_vu | {baseline_rps} req/s | From baseline run        |
| normal_vus          | {normal_vus}         | {normal_vus_calculation} |
| peak_vus            | {peak_vus}           | {peak_vus_calculation}   |

### Stage Table

| Stage # | Name           | Target VUs    | Duration           | Goal           |
| ------- | -------------- | ------------- | ------------------ | -------------- |
| 1       | {stage_1_name} | {stage_1_vus} | {stage_1_duration} | {stage_1_goal} |
| 2       | {stage_2_name} | {stage_2_vus} | {stage_2_duration} | {stage_2_goal} |
| N       | {stage_n_name} | {stage_n_vus} | {stage_n_duration} | {stage_n_goal} |

**Total duration:** {total_duration}

### Think Time

**Value:** {think_time_seconds} seconds (randomised: {think_time_range})

**Reasoning:** {think_time_reasoning}

### Abort Conditions _(stress and spike tests only)_

| Condition   | Threshold                | Action                         |
| ----------- | ------------------------ | ------------------------------ |
| Error rate  | {abort_error_threshold}% | `abortOnFail: true`, delay 30s |
| p95 latency | {abort_p95_threshold} ms | `abortOnFail: true`, delay 30s |

_(Remove this section if `test_type` is load or soak.)_

## 3. Threshold Configuration

| k6 Threshold              | Value                    | Source                                      |
| ------------------------- | ------------------------ | ------------------------------------------- |
| `http_req_duration p(95)` | < {p95_threshold_ms} ms  | `perf-config.json slo.p95_threshold_ms`     |
| `http_req_failed rate`    | < {error_rate_threshold} | `perf-config.json slo.error_rate_threshold` |

_These values must match `perf-config.json slo.*` exactly._

## 4. Request Payload

**Method:** `{method} {endpoint_path}`

**Headers:**

```
Content-Type: application/json
Authorization: Bearer <token>   ← omit if no auth required
```

**Body:**

```json
{request_body_json}
```

**Field descriptions:**

| Field     | Type     | Source     | Notes     |
| --------- | -------- | ---------- | --------- |
| {field_1} | {type_1} | {source_1} | {notes_1} |

## 5. Auth Strategy

**Selected strategy:** {auth_strategy_name}

**Reasoning:** {auth_strategy_brief_reasoning}

_Full analysis: `plan/auth-strategy.md`_

## 6. CSV Schema Summary

**File:** `plan/data/{group}.csv`

| Column  | Type     | Required | Description |
| ------- | -------- | -------- | ----------- |
| {col_1} | {type_1} | Yes/No   | {desc_1}    |

**Required row count:** {required_rows}  
**Calculation method:** {row_count_method}

_Full schema and example rows: `plan/data/csv-schema.md`_

## 7. Output Configuration

| Output Type     | Configuration     | Purpose            |
| --------------- | ----------------- | ------------------ |
| {output_1_type} | {output_1_config} | {output_1_purpose} |

_e.g. HTML report via `handleSummary()`, JSON stream via `--out` json, CSV via `--out` csv_

## 8. Known Constraints & Mitigations

| Constraint     | Source     | Mitigation     |
| -------------- | ---------- | -------------- |
| {constraint_1} | {source_1} | {mitigation_1} |

_(Common constraints: DB write serialisation, account lockout policy, JWT TTL shorter than soak duration, unique email exhaustion.)_

## 9. Human Gate Review Notes

_(AI populates this section with any change requests made at Human Gate 1 and how they were addressed.)_

{human_gate_notes}

_This document is the approved workload model. The full test script is built from this document._
