# CSV Schema — {group_name}

> **Generated by:** perf-plan  
> **Date:** {created_at}  
> **Endpoint:** `{method} {endpoint_path}`  
> **Test Type:** {test_type}  
> **Auth Strategy:** {auth_strategy_name}

## 1. Column Schema

| Column  | Type     | Required | Format / Constraints | Source     |
| ------- | -------- | -------- | -------------------- | ---------- |
| {col_1} | {type_1} | Yes/No   | {format_1}           | {source_1} |
| {col_2} | {type_2} | Yes/No   | {format_2}           | {source_2} |

**Column sources:**

- `credentials` = obtained from auth strategy (email/password for login, token for Strategy 3)
- `db_seeded` = real ID created by `seed-data.js` and read back from the API
- `generated` = synthetic value that does not require a DB lookup (e.g. generated email pattern)
- `static` = same value on every row (e.g. fixed quantity or price)

## 2. Row Count Calculation

**Data consumption category:** {data_category}

_(Category A — Reusable rows, or Category B — Write-once rows)_

**Calculation:**

```
{row_count_formula_with_actual_values}
```

| Variable                  | Value                  | Source                                 |
| ------------------------- | ---------------------- | -------------------------------------- |
| peak_vus                  | {peak_vus}             | `test-plan.md`                         |
| test_duration_seconds     | {test_duration_s}      | `test-plan.md`                         |
| avg_response_time_seconds | {avg_response_s}       | `perf-config.json baseline.p50_ms`     |
| think_time_seconds        | {think_time_s}         | `test-plan.md`                         |
| iterations_per_vu         | {iterations_per_vu}    | Calculated                             |
| total_iterations          | {total_iterations}     | Calculated                             |
| buffer_factor             | {buffer_factor}        | 1.15 (Category A) or 1.30 (Category B) |
| **recommended_rows**      | **{recommended_rows}** | **Final value**                        |

## 3. Uniqueness Requirements

| Column  | Unique Required | Pattern / Guarantee    |
| ------- | --------------- | ---------------------- |
| {col_1} | Yes/No          | {uniqueness_pattern_1} |

_(For write-once fields, state the exact generation pattern, e.g.: `perf_{run_timestamp}_{zero_padded_index}@test.local`)_

## 4. Example Rows

```csv
{header_row}
{example_row_1}
{example_row_2}
{example_row_3}
```

_(Use realistic values — not placeholder text like `user@example.com` or `product_name`. Values must conform to the format constraints in the schema table above.)_

## 5. Seed Data Requirements

**Records that must exist in the DB before generate-data.js runs:**

| Record Type     | Count Required | Created by     |
| --------------- | -------------- | -------------- |
| {record_type_1} | {count_1}      | `seed-data.js` |

_(If no seed data is required — e.g. for a registration endpoint — state "No seed required. generate-data.js produces all data synthetically.")_

## 6. Cleanup Procedure

To remove test data created by seed-data.js and visible in the CSV:

```
{cleanup_command_or_instructions}
```

_(e.g. API calls to delete seeded users, or direct DB commands if permitted in the test environment. Document this so test data does not persist and interfere with subsequent runs.)_

## 7. Human Gate Review Notes

_(AI populates this section with any change requests made at Human Gate 2 and how they were addressed.)_

{human_gate_notes}
